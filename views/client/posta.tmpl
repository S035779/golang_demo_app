{{ define "title" }}PostA{{ end }}

{{ define "content" }}

<form action="{{ .url  }}" method="POST">
  <p>Check some colors</p>
  <label for="red">Red</label>
  <input type="checkbox" name="colors[]" value="{{ .color_a }}" id="red" />
  <label for="green">Green</label>
  <input type="checkbox" name="colors[]" value="{{ .color_b }}" id="green" />
  <label for="blue">Blue</label>
  <input type="checkbox" name="colors[]" value="{{ .color_c }}" id="blue" />
  <input type="submit" />
</form>
{{ end }}
